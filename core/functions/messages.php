<?php
function count_messages($employee_id) {
	//$employee_id = mysql_real_escape_string($employee_id);
	//$query = mysql_query("SELECT count(message_id) as count_messages FROM messages WHERE message_to='$employee_id'");
	//$row = mysql_fetch_array($query);
	//return (mysql_num_rows($query) > 0 ) ? $row['count_messages'] : 0;
}
?>
